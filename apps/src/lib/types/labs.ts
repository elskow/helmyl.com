export interface LabProject {
	name: string;
	description: string;
	version: string;
	slug: string;
	editedAt: string;
	createdAt: string;
	author?: string;
	homepage?: string;
	repository?: string;
}
