{
	"name": "helmyl-web-monorepo",
	"private": true,
	"scripts": {
		"dev": "pnpm --filter apps run dev",
		"clean": "pnpm --filter \"@labs/*-labs\" run clean && pnpm --filter apps run clean",
		"clean:labs": "pnpm --filter \"@labs/*-labs\" run clean",
		"clean:web": "pnpm --filter apps run clean",
		"build:labs": "pnpm --filter \"@labs/*-labs\" run clean && pnpm --filter \"@labs/*-labs\" run build",
		"build:web": "pnpm --filter apps run build",
		"build": "pnpm build:labs && pnpm build:web",
		"preview": "pnpm --filter apps preview"
	}
}
